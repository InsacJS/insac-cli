const Insac = require(insac).Insac

const service = new Insac()

// |=============================================================|
// |-------------- MÓDULOS DE SOLO LECTURA ----------------------|
// |=============================================================|

// Ejemplo:
// service.addModule('auth', { setup: false })

// |=============================================================|
// |-------------- MÓDULOS PRINCIPALES --------------------------|
// |=============================================================|

// <!-- [CLI] - [MODULE] --!> //

// |=============================================================|
// |-------------- EJECUCIÓN ------------------------------------|
// |=============================================================|

service.init()

module.exports = service
