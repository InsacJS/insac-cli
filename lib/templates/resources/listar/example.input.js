INPUT.{{key}} = {
    headers: {
      authorization: Field.USER_AUTHORIZATION
    },
    query: {
      fields : Field.FIELDS,
      order  : Field.ORDER,
      limit  : Field.LIMIT,
      page   : Field.PAGE
    }
  }
