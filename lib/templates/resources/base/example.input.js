module.exports = (data) => {
  const _r = `// const { Field } = require('insac')
module.exports = (app) => {
  const INPUT = {}

  // <!-- [CLI] - [COMPONENT] --!> //

  return INPUT
}
`
  return _r
}
