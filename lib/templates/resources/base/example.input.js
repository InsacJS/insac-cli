module.exports = (data) => {
  const _r = `// const Field = require(insac).Field
// const THIS = require(insac).THIS

module.exports = (app) => {
  const INPUT = {}

  // <!-- [CLI] - [COMPONENT] --!> //

  return INPUT
}
`
  return _r
}
