module.exports = (data) => {
  const _r = `// const { Field } = require('insac')

module.exports = (app) => {
  const OUTPUT = {}

  // <!-- [CLI] - [COMPONENT] --!> //

  return OUTPUT
}
`
  return _r
}
