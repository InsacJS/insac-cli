module.exports = (data) => {
  const _r = `// const { NotFound } = require(insac).errors

module.exports = (app) => {
  const MIDDLEWARE = {}

  // <!-- [CLI] - [COMPONENT] --!> //

  return MIDDLEWARE
}
`
  return _r
}
