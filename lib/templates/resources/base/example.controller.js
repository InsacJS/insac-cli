module.exports = (data) => {
  const _r = `// const util = require(insac).util

module.exports = (app) => {
  const CONTROLLER = {}

  // <!-- [CLI] - [COMPONENT] --!> //

  return CONTROLLER
}
`
  return _r
}
