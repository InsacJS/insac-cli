module.exports = (data) => {
  const _r = `const { Field, THIS } = require(insac)

module.exports = (app) => {
  const OUTPUT = {}

  // <!-- [CLI] - [ROUTE] --!> //

  return OUTPUT
}
`
  return _r
}
