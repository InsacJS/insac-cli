module.exports = (data) => {
  const _r = `const errors = require(insac).errors

module.exports = (app) => {
  const MIDDLEWARE = {}

  // <!-- [CLI] - [ROUTE] --!> //

  return MIDDLEWARE
}
`
  return _r
}
