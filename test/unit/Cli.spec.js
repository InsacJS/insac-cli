/* global describe it expect */
describe('\n - Archivo insac-cli.js\n', () => {
  describe(` Método: constructor`, () => {
    it('Instanciando un objeto sin parámetros', () => {
      expect(true).to.equal(true)
    })
  })
})
