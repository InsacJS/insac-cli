const util = require(insac).util

// |=============================================================|
// |-------------- CONSTANTES -----------------------------------|
// |=============================================================|

// util.INTEGER_MAX_VALUE = 2147483647

// |=============================================================|
// |-------------- VARIOS ---------------------------------------|
// |=============================================================|

// util.find      = (dirPath, ext, onFind) => {}
// util.findAsync = async function findAsync (dirPath, ext, onFind) {}
// util.timer     = (timeout) => {}
// util.cmd       = (command, executePath) => {}
// util.json      = (data) => {}
// util.log       = (data) => {}
// util.obj       = (data, properties) => {}

// |=============================================================|
// |-------------- DIRECTORIOS ----------------------------------|
// |=============================================================|

// util.isDir = (dirPath) => {}
// util.mkdir = (dirPath) => {}
// util.rmdir = (dirPath) => {}

// |=============================================================|
// |-------------- ARCHIVOS -------------------------------------|
// |=============================================================|

// util.isFile     = (filePath) => {}
// util.readFile   = (filePath) => {}
// util.writeFile  = (filePath, content) => {}
// util.removeFile = (filePath) => {}
// util.copyFile   = (sourcePath, targetPath) => {}

// |=============================================================|
// |-------------- PERSONALIZADOS -------------------------------|
// |=============================================================|

module.exports = util
