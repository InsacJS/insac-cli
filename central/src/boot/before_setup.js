module.exports = (app) => {
  // |=============================================================|
  // |---- TAREAS A REALIZAR ANTES DE INSTALAR LOS MÓDULOS --------|
  // |=============================================================|

  require('../tools/Field.js')
}
